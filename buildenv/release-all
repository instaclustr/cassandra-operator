#!/bin/bash -xue

#docker push "${DOCKER_REPO}base-openjre:latest"
docker push "${DOCKER_REPO}cassandra-operator:latest"
docker push "${DOCKER_REPO}cassandra-sidecar:latest"
docker push "${DOCKER_REPO}cassandra:latest"
docker push "${DOCKER_REPO}cassandra:3.11.3"