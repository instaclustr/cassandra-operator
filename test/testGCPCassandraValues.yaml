replicaCount: 3

image:
  repository: gcr.io/cassandra-operator/cassandra
  tag: latest

imagePullPolicy: Never

resources:
  limits:
    memory: 512Mi
  requests:
    memory: 512Mi
dataVolumeClaim:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi