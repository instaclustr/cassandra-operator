#!/usr/bin/env bash

#unit tests
#TODO: gcp specific tests

helm install -f testGCPCassandraValues.yaml -n cassandra-default --wait ../helm/cassandra
export CASSANDRA_CLUSTER=cassandra-default

cd ../java
mvn test -q -Dgroups=integration
cd ../test
