apiVersion: v1
kind: Service
metadata:
  name: {{ template "cassandra-backup-schedule.fullname" . }}
  labels:
    app: {{ template "cassandra-backup-schedule.name" . }}
    chart: {{ template "cassandra-backup-schedule.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "cassandra-backup-schedule.name" . }}
    release: {{ .Release.Name }}
